---
// The swipe — citron highlighter behind a run of type. The brand's
// signature gesture. Used sparingly (one per view).
---
<span class="swipe"><span class="swipe__mark"></span><span class="swipe__txt"><slot /></span></span>
